{
  "name": "memran/php-testify",
  "description": "A modern, expressive testing library for PHP that makes writing tests enjoyable and readable.",
  "keywords": [
    "phpunit",
    "test",
    "vitest",
    "jest",
    "watch",
    "bdd",
    "expect"
  ],
  "type": "library",
  "license": "MIT",
  "authors": [
    {
      "name": "Mohammad Emran",
      "email": "memran.dhk@gmail.com"
    }
  ],
  "require": {
    "php": "^8.2",
    "phpunit/phpunit": "^11.0"
  },
  "require-dev": {
    "friendsofphp/php-cs-fixer": "^3.76",
    "phpstan/phpstan": "^1.12"
  },
  "autoload": {
    "psr-4": {
      "Testify\\": "src/"
    },
    "files": [
      "src/functions.php"
    ]
  },
  "bin": [
    "bin/testify"
  ],
  "autoload-dev": {
    "psr-4": {
      "Tests\\": "tests/"
    }
  },
  "scripts": {
    "test": "vendor/bin/phpunit --configuration phpunit.xml.dist",
    "test:coverage": "vendor/bin/phpunit --configuration phpunit.xml.dist --coverage-text",
    "test:package": "php bin/testify --no-colors",
    "analyse": "vendor/bin/phpstan analyse --configuration=phpstan.neon.dist --no-progress --debug",
    "lint": "vendor/bin/php-cs-fixer fix --dry-run --diff --verbose --sequential",
    "fix": "vendor/bin/php-cs-fixer fix --verbose --sequential",
    "ci": [
      "@lint",
      "@analyse",
      "@test",
      "@test:package"
    ]
  },
  "config": {
    "sort-packages": true
  },
  "minimum-stability": "stable",
  "prefer-stable": true
}
